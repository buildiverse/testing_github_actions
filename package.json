{
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.6.2"
  },
  "scripts": {
    "prepare": "husky",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "ruff": "ruff check .",
    "ruff:fix": "ruff check . --fix"
  }
}
